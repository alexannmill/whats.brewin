

const PostContainer = (props) => {
  

  //5 random numbers between 1-100 for db
  //axios to back end for post
  //route for post using params to render
  //each post render info
  //query for each post 
  //post container the map through all post set
  
  
    return (
      <div></div>
    );
  };

  export default PostContainer;